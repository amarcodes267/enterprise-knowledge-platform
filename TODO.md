# TODO

## Premium UI (frontend only)
- [ ] Inspect remaining UI components used by /chat, /search, /upload (UploadBox, SearchBar, Sidebar, Message, FileCard, etc.)
- [ ] Implement global design system (CSS variables, base typography, focus rings, container, utilities) in `frontend/src/index.css`
- [ ] Upgrade Navbar markup (remove duplicate import, use premium layout rules) and polish `frontend/src/styles/Navbar.css`
- [x] Polish Home page visuals: Hero / FeatureCards / Footer / Home sections CSS
- [x] Implement global premium design system basics (index.css) + polish Navbar

- [x] Upgrade /chat page layout: style `ChatBox.jsx` + related components for premium look
- [x] Upgrade /search page layout: replace inline styles with premium classes
- [x] Upgrade /upload page layout: style `UploadBox.jsx` + related components
- [ ] Run `npm run build` and/or `npm run dev` in `frontend/` to validate build


